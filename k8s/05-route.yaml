apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: notizen-app
  namespace: YOUR_NAMESPACE
spec:
  to:
    kind: Service
    name: notizen-app
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
